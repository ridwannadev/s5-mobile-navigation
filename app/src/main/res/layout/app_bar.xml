<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/app_bar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:padding="@dimen/nav_margin"
    android:background="?android:attr/colorPrimary"
    android:backgroundTint="@color/white">

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:src="@drawable/ic_youtubelogodark"
        android:layout_centerVertical="true"
        />

    <ImageView
        android:id="@+id/accountIconImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_search"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/searchIconImageView"
        android:layout_toStartOf="@id/searchIconImageView"
        android:layout_marginEnd="@dimen/nav_margin"
        android:layout_marginRight="@dimen/nav_margin"/>

    <!-- Ikon Pencarian -->
    <ImageView
        android:id="@+id/searchIconImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_account"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginEnd="@dimen/nav_margin"
        android:layout_marginRight="@dimen/nav_margin" />

    <!-- Ikon Notifikasi -->
    <ImageView
        android:id="@+id/notificationsIconImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_notification"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/accountIconImageView"
        android:layout_toStartOf="@id/accountIconImageView"
        android:layout_marginEnd="@dimen/nav_margin"
        android:layout_marginRight="@dimen/nav_margin" />

    <!-- Ikon Hubungkan Perangkat -->
    <ImageView
        android:id="@+id/connectDeviceIconImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_connect"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/notificationsIconImageView"
        android:layout_toStartOf="@id/notificationsIconImageView"
        android:layout_marginEnd="@dimen/nav_margin"
        android:layout_marginRight="@dimen/nav_margin" />

</RelativeLayout>
